# TeaVision Edge

On Windows, double-click **Start TeaVision.cmd** to open the application.
The launcher uses `EdgeNode\.venv`, so switching Python environments for other
projects does not affect startup.

From PowerShell:

```powershell
& .\EdgeNode\.venv\Scripts\python.exe .\EdgeNode\main.py
```
